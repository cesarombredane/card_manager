{
  "name": "pokemon-card-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w client",
    "dev": "devspace dev",
    "deploy": "devspace deploy"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
