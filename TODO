app:
- del all app
- create home page and define style
- create a page listing the sets and series for one region (switch region with a button)
- create a page for all the cards in a set with a search bar and filters
- create a detail card page to see a specific card detail (and it's variants)
- create a page for search of cards (can be per pokemon, artist)
- smooth transition between pages

scalping:
- scalp data from tcgdex to start with
- see if using tcgdex image is apropriate or if we need to host our own images

testing:
- test the app and fix bugs and incoherences

other features:
- add a collectrion feature where i can add cards to my collection and see my collection stats
- add a binder creation feature where i can create a binder with my collection and see it in a nice way
